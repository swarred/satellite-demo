# Allow insecure (HTTP) pulls from the build host's local registry.
# The local registry runs at 192.168.122.1:5000 on the KVM bridge —
# reachable from the satellite VM even during DDIL (OCP routes blocked,
# KVM bridge is not).
[[registry]]
location = "192.168.122.1:5000"
insecure = true
